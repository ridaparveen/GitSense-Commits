# Activity Summary for 5/5/2026

## 1:06:25 PM
The code change log primarily focuses on two JavaScript files, `pattern.js` and `sunArray.js`, located within a `DSA_Learning` directory. The changes span from April 28, 2026, to May 1, 2026, indicating an active development and learning process for Data Structures and Algorithms.

**File: `c:\Users\ridap\OneDrive\Documents\DSA_Learning\pattern.js`**

This file is dedicated to printing various star patterns using nested `for` loops.

*   **4/28/2026, 12:42:33 AM:** An initial function `printPattern()` was introduced to print a decreasing star pattern (5 down to 1 star).
*   **4/28/2026, 12:49:31 AM - 12:51:06 AM:**
    *   A new function `printLoop3()` was added, initially duplicating the `printPattern()` logic.
    *   It then underwent several modifications to correctly implement a pattern that first decreases stars and then increases them (e.g., `*****` down to `*`, then `*` up to `*****`). This involved fixing loop conditions and variable names (`row2`).
*   **4/28/2026, 12:50:06 AM:** The original `printPattern()` function was commented out.
*   **4/28/2026, 1:04:00 AM:** The `printLoop3()` function was commented out. A more complex pattern generation function, `printPattern4()`, was introduced. This new function aimed to print a pattern involving stars and spaces on both sides, and it was incrementally refined over the next few entries.
*   **4/28/2026, 1:04:14 AM - 1:04:59 AM:** `printPattern4()` was refined by:
    *   Adding the `console.log(row)` statement inside the main loop.
    *   Adjusting loop conditions for printing stars and dynamic spaces (`j <= i` for stars, `j <= n - i` for spaces) to create the desired spaced-star pattern.

**File: `c:\Users\ridap\OneDrive\Documents\DSA_Learning\sunArray.js`**

This file shows a progression of understanding and implementing array-related algorithms.

*   **4/28/2026, 1:54:40 AM:** An initial `subArray()` function was defined, intended to process sub-arrays but initially had incorrect logic, concatenating elements repeatedly.
*   **4/28/2026, 1:55:12 AM - 2:02:09 AM:** This period shows extensive debugging and refinement of the `subArray()` function:
    *   Multiple `console.log` statements were added and removed to trace loop indices and string concatenation.
    *   The `j` loop was correctly started from `i` (`j = i`), and a third nested `k` loop (`k = i` to `j`) was introduced to correctly extract elements for each sub-array.
    *   The `str` variable was moved inside the `j` loop (`let str = ""`) to ensure it resets for each unique sub-array, making the function correctly print all contiguous sub-arrays.
    *   The `length` parameter in the function call was adjusted or removed/re-added as `subArray([1,2,3,4,5], 5)`.
*   **4/28/2026, 2:14:08 AM:** The `subArray(arr, length)` function was commented out. A new `subArray(arr)` function was introduced, which automatically derives `length` from `arr.length`, and was used with an array containing negative numbers.
*   **4/28/2026, 2:15:39 AM - 2:18:56 AM:** The `subArray(arr)` function was modified to attempt to calculate the maximum sum of a sub-array. This phase involved several incorrect attempts at implementing the logic, including misplacing `maxSum` and `currentSum` updates and confusing their roles, even renaming the function to `subMArray` at one point while still calling `subArray`.
*   **4/28/2026, 2:20:50 AM:** The previous incorrect `subArray` code was commented out. A new `maxSubArray(arr)` function was introduced, which correctly implements Kadane's algorithm to find the maximum sum of a contiguous sub-array. This was a significant correction to the previous attempts.
*   **4/28/2026, 2:29:46 AM - 2:31:17 AM:** The `maxSubArray` function was commented out. A new function `sumOfOddSubArray()` was added, which simply iterated through an array `[1,4,2,5,3]` and logged either `i` or `i+1` (toggling between them).
*   **4/30/2026, 8:06:09 PM:** The `sumOfOddSubArray()` function was commented out. A new utility function `isPalindrome(str)` was added, which correctly checks if a string is a palindrome by reversing it and comparing.
*   **4/30/2026, 8:06:23 PM - 8:06:30 PM:** A minor typo in the `isPalindrome` function call (missing quotes around "madam") was introduced and quickly corrected.
*   **4/30/2026, 8:11:11 PM:** A `findLargest(arr)` function was added, which finds the largest element in an array by sorting it.
*   **4/30/2026, 8:11:38 PM - 5/1/2026, 7:49:09 AM:** The `findLargest` function was debugged with `console.log` statements inside the sort comparator. Temporarily, the sorting logic was commented out, making the function incorrect, but it was subsequently uncommented and assigned to a new variable `data` to ensure correct functionality.

**Patterns and Recurring Elements:**

*   **DSA Learning Focus:** Both files consistently demonstrate an exploration of fundamental Data Structures and Algorithms concepts, specifically pattern generation and array manipulation techniques.
*   **Iterative Development and Refinement:** Code is frequently added, then modified, debugged, and improved through multiple commits, often involving small, incremental changes to loop conditions or variable assignments.
*   **Commenting Out Old Code:** A strong recurring pattern is the commenting out of previous function implementations or blocks of code when new versions or different algorithmic approaches are introduced. This allows for historical context without interfering with the current active code.
*   **Debugging with `console.log`:** Extensive use of `console.log` statements at various points in the code, especially within loops and conditional blocks, indicates a hands-on debugging approach to understand program flow and variable states.
*   **Evolution of Algorithms:** In `sunArray.js`, there's a clear progression from basic array iteration to generating sub-arrays, then attempting to solve the maximum sub-array sum problem (initially incorrectly, then correctly with Kadane's algorithm), and finally adding general utility functions.
*   **Syntax and Logic Correction:** Minor syntax errors (like missing quotes) and more significant logical errors (like incorrect loop bounds or algorithm implementation) are frequently introduced and quickly corrected in subsequent entries.