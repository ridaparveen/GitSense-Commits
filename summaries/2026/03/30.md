# Activity Summary for 3/30/2026

## 1:38:41 PM
The logs detail code changes across two JavaScript files: `dsaMock.js` and `leetCode.js`, both located in `c:\Users\ridap\OneDrive\Documents\DSA_Learning\`.

### File-Specific Updates:

1.  **`c:\Users\ridap\OneDrive\Documents\DSA_Learning\dsaMock.js`**
    This file was actively modified on **March 14, 2026**, within a short span from **11:40:43 PM to 11:41:52 PM**. The content primarily explores JavaScript's asynchronous behavior and event loop:
    *   **Initial Change (11:40:43 PM):** Introduced an example demonstrating the order of `console.log`, `setTimeout`, and `Promise.resolve().then()` execution. Previous code snippets for finding the largest value in an array and string iteration were commented out.
    *   **Second Change (11:41:25 PM):** The event loop example was commented out. A new code block was added to illustrate the common closure issue with `var` in a loop combined with `setTimeout`, showing `for (var i = 0; i < 3; i++) { setTimeout(() => console.log(i), 100); }`.
    *   **Third Change (11:41:52 PM):** The `setTimeout` loop example was commented out. A new `async/await` example was introduced, demonstrating the interaction of `setTimeout`, `async` functions, and `Promise.resolve()`, with specific `console.log` statements ("A", "B", "C", "D") to trace execution flow.

2.  **`c:\Users\ridap\OneDrive\Documents\DSA_Learning\leetCode.js`**
    This file saw changes on **March 25, 2026**, from **8:04:50 AM to 8:07:39 AM**, focusing on array concatenation:
    *   **Initial Change (8:04:50 AM):** A `concatinationOfArray` function was defined using the spread operator (`[...x, ...x]`) to duplicate an array, returning the concatenated result.
    *   **Second Change (8:05:24 AM):** The `concatinationOfArray` function was updated to `console.log` the result directly instead of just returning it.
    *   **Third Change (8:07:04 AM):** The spread operator version of the function was commented out. A new implementation, as an arrow function, was added that achieved array concatenation using the `concat()` method (`y.concat(y)`), also logging the result.
    *   **Fourth Change (8:07:39 AM):** Comments were added to clearly label the two different approaches: "using spread operator" (for the commented-out version) and "using concat method" (for the active version).

### Patterns and Recurring Elements:

*   **Learning/Experimentation Environment:** Both files appear to be used as a personal sandbox for learning or practicing JavaScript concepts, suggested by their paths (`DSA_Learning`) and the nature of the code (exploring language features, common problems).
*   **Progressive Code Examples:** A strong pattern is the successive commenting out of previous code examples or implementations when introducing new ones within the same file. This allows for retaining past attempts or alternative solutions while keeping the active code focused on the latest concept.
*   **Focused Activity Bursts:** The timestamps indicate concentrated periods of work for each file, suggesting dedicated short sessions for exploring specific JavaScript topics (e.g., event loop on one day, array methods on another).
*   **JavaScript Fundamentals:** The content covers core JavaScript concepts: asynchronous programming (`setTimeout`, `Promise`, `async/await`), variable scoping issues (`var` in loops), and fundamental array manipulations (concatenation, spread operator, `concat()` method).