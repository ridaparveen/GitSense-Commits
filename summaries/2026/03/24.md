# Activity Summary for 3/24/2026

## 12:48:28 PM
The file `c:\Users\ridap\OneDrive\Documents\DSA_Learning\dsaMock.js` was consistently updated throughout the log. The changes reflect an iterative exploration of JavaScript's asynchronous execution model and event loop behavior.

**File-specific Updates for `c:\Users\ridap\OneDrive\Documents\DSA_Learning\dsaMock.js`:**
*   **3/14/2026, 11:40:43 PM:** The file contained commented-out code snippets for array manipulation (finding largest value) and string iteration. The active code introduced a basic demonstration of the event loop with `console.log`, `setTimeout`, and `Promise.resolve().then()`.
*   **3/14/2026, 11:41:25 PM:** The previous active event loop demonstration was commented out. A new code snippet was added to illustrate the closure issue with `var` inside a `for` loop combined with `setTimeout`.
*   **3/14/2026, 11:41:52 PM:** The `setTimeout` loop example was commented out. The final update introduced a more complex asynchronous example involving `setTimeout`, an `async` function, `await Promise.resolve()`, and `console.log` statements, further exploring the interplay of macro and micro tasks in the event loop.

**Patterns and Recurring Elements:**
The primary pattern observed is a sequential focus on understanding different aspects of JavaScript's asynchronous nature. Each new active code block builds upon or explores a different facet of the event loop, `setTimeout`, Promises, or `async/await`. A consistent workflow involves commenting out the previously active asynchronous code snippet before introducing a new one, suggesting an iterative testing or learning process within the same file. The older, basic DSA-related code at the beginning of the file remains commented out across all entries, indicating a dedicated section for asynchronous JavaScript experimentation at the end of the file.

## 1:48:32 PM
The file `c:\Users\ridap\OneDrive\Documents\DSA_Learning\dsaMock.js` was updated multiple times in quick succession on 3/14/2026.

**File-specific updates for `c:\Users\ridap\OneDrive\Documents\DSA_Learning\dsaMock.js`:**

*   **3/14/2026, 11:40:43 PM:** The file contained commented-out code snippets related to basic DSA tasks (finding the largest value in an array, character count in a string). The active code demonstrated the JavaScript event loop order with `console.log`, `setTimeout`, and `Promise.resolve().then()`, showing the microtask queue preceding the macrotask queue.
*   **3/14/2026, 11:41:25 PM:** The previous event loop example was commented out. New active code was introduced to illustrate a common closure problem with `var` in a `for` loop combined with `setTimeout`, where the loop variable's final value is captured.
*   **3/14/2026, 11:41:52 PM:** The `setTimeout` loop example was commented out. The latest active code demonstrates the execution order of `setTimeout`, `async/await` functions, and `Promise.resolve()`, further exploring JavaScript's asynchronous behavior and event loop.

**Patterns and recurring elements:**

A clear pattern emerges where previously active code is commented out before new code is added, suggesting a workflow of testing different JavaScript concepts sequentially within the same file. Each active code block focuses on illustrating a specific aspect of JavaScript's asynchronous execution, concurrency model, or event loop behavior (`setTimeout` vs. `Promise`, `var` closure issues, `async/await` interactions). The timestamps indicate rapid iteration through these concepts.