# Activity Summary for 3/28/2026

## 3:41:05 PM
The codebase reflects development across two distinct JavaScript files, `dsaMock.js` and `leetCode.js`, showcasing different areas of JavaScript problem-solving and concept exploration.

### `c:\Users\ridap\OneDrive\Documents\DSA_Learning\dsaMock.js`

This file was actively modified on **March 14, 2026**, within a concentrated period between **11:40:43 PM** and **11:41:52 PM**. The primary focus of this file is to explore and demonstrate various aspects of JavaScript's asynchronous execution and event loop.

*   Initially, the file contained commented-out code snippets for basic array manipulation (finding the largest value) and string iteration, suggesting earlier DSA practice.
*   The first active change introduced a classic event loop example, using `console.log`, `setTimeout`, and `Promise.resolve().then()` to illustrate the execution order of synchronous code, macrotasks, and microtasks.
*   Subsequently, this code was commented out, and a new example was added. This focused on the behavior of `setTimeout` within a `for` loop using `var`, demonstrating common JavaScript closure and `var` scoping issues in asynchronous contexts.
*   Finally, this `for` loop example was also commented out, and the file shifted to an `async/await` example combined with `setTimeout`. This further explored the event loop, specifically the interaction between `setTimeout` (macrotask) and `await Promise.resolve()` (microtask).

A consistent pattern in `dsaMock.js` is the iterative testing of JavaScript's asynchronous mechanisms. Each new example comments out the previous active one, suggesting a structured approach to learning or demonstrating specific event loop behaviors.

### `c:\Users\ridap\OneDrive\Documents\DSA_Learning\leetCode.js`

This file was modified on **March 25, 2026**, between **8:04:50 AM** and **8:07:39 AM**, indicating a focused session on a particular coding problem. The central theme is finding different ways to concatenate an array with itself.

*   The file started with a function `concatinationOfArray` that took an array `x` and returned a new array by concatenating `x` with itself using the spread operator (`[...x, ...x]`). The initial version called the function but didn't log its result.
*   A subsequent update at **8:05:24 AM** modified the function to `console.log` the result directly, making the output visible.
*   At **8:07:04 AM**, the spread operator implementation was commented out, and a new `concatinationOfArray` function (defined as an arrow function) was introduced. This version achieved the same array concatenation using the `concat()` method (`y.concat(y)`), logging the result.
*   The final change at **8:07:39 AM** involved adding comments to clearly label the two different approaches: "using spread operator" and "using concat method."

The recurring pattern in `leetCode.js` is the exploration and comparison of different methods to solve the same array manipulation problem, demonstrating an understanding of various JavaScript features for array handling.