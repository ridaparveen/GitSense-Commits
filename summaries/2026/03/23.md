# Activity Summary for 3/23/2026

## 1:11:24 PM
The `dsaMock.js` file, located at `c:\Users\ridap\OneDrive\Documents\DSA_Learning\dsaMock.js`, was updated three times within a short period on March 14, 2026, consistently demonstrating different aspects of JavaScript's asynchronous execution and event loop.

**File-specific updates for `c:\Users\ridap\OneDrive\Documents\DSA_Learning\dsaMock.js`:**

*   **3/14/2026, 11:40:43 PM:** The file initially contained active code illustrating basic event loop behavior with `console.log("1")`, `setTimeout(() => console.log("2"))`, `Promise.resolve().then(() => console.log("3"))`, and `console.log("4")`. Prior code for finding the largest value in an array and a commented-out loop for string iteration remained at the top.
*   **3/14/2026, 11:41:25 PM:** The previous event loop example was commented out. New active code was introduced: a `for` loop using `var` with `setTimeout` inside (`for (var i = 0; i < 3; i++) { setTimeout(() => console.log(i), 100); }`), often used to demonstrate variable hoisting and closure issues with `setTimeout` in loops.
*   **3/14/2026, 11:41:52 PM:** All prior active code (including the `for` loop with `setTimeout`) was commented out. The latest change introduced another sequence exploring the event loop with `setTimeout(() => console.log("A"), 0);`, an `async` function (`test`) containing `console.log("B")`, `await Promise.resolve()`, `console.log("C")`, and a final `console.log("D")`, further illustrating the interplay of macrotasks, microtasks, and async/await.

**Patterns and recurring elements:**

The most prominent pattern is the repeated focus on JavaScript's asynchronous execution model, including the event loop, `setTimeout`, `Promise`, and `async/await`. Each update comments out the previous active code block and introduces a new, distinct example designed to test or demonstrate a specific aspect of asynchronous behavior, primarily involving the order of `console.log` statements. This suggests the file is being used as a learning or experimentation sandbox for understanding these complex JavaScript concepts. Older, non-asynchronous code (like finding the largest value in an array) consistently remains commented out at the beginning of the file throughout these changes.